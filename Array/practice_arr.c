// wap to print 1D array.

// wap to print your name in an array .
// wap to except an integers an store them in an array called num and also them.

#include <stdio.h>
int main()
{
    int a[6] = {2, 3, 4, 5, 6, 7};
    for (int i = 0; i < 6; i++)
        printf("%d\t", a[i]);
    return 0;
}